export const metadata = {
  title: 'Shopping Cart | Banner Direct',
  description: 'Review your custom banner order. Fast shipping across Canada.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function CartLayout({ children }) {
  return children;
}
