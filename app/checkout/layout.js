export const metadata = {
  title: 'Checkout | Banner Direct',
  description: 'Complete your custom banner order. Secure checkout with fast shipping across Canada.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
