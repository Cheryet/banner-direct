export const metadata = {
  title: 'Sign In | Banner Direct',
  description: 'Sign in to your Banner Direct account to manage orders and track shipments.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function AuthLayout({ children }) {
  return children;
}
